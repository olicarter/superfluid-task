import styled from 'styled-components'

export const Row = styled.div({
  alignItems: 'center',
  display: 'flex',
  padding: '0 0 0.5rem',
})

export const IconWrapper = styled.div({
  display: 'flex',
  paddingRight: '0.5rem',
})
